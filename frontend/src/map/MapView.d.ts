import { MapViewProps } from './types';

/**
 * Resolved by the bundler to MapView.web.tsx or MapView.native.tsx.
 * This declaration gives the shared import path a single type.
 */
declare const MapView: (props: MapViewProps) => JSX.Element;
export default MapView;
